<Window x:Class="TeknoParrotUi.Views.PoliciesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TeknoParrotUi.Views"
        xmlns:p="clr-namespace:TeknoParrotUi.Properties"
        mc:Ignorable="d"
        Title="{x:Static p:Resources.PoliciesWindowTitle}" Height="300" Width="600" WindowStyle="None" WindowStartupLocation="CenterScreen">
    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="30,20,30,0">
            <TextBlock FontSize="16" FontWeight="Bold" Text="Updated Policies"/>
            <TextBlock FontSize="12" Margin="0,10,0,0" TextWrapping="Wrap" Foreground="#333333">
                We've updated our policies.
            </TextBlock>
        </StackPanel>
        
        <!-- Links -->
        <StackPanel Grid.Row="1" Margin="30,20,30,0">
            <TextBlock FontSize="11">
                <Hyperlink Name="PoliciesLink" Click="PoliciesLink_Click" Foreground="#0066CC" TextDecorations="Underline">
                    View policies
                </Hyperlink>
            </TextBlock>
        </StackPanel>
        
        <!-- Agreement Text -->
        <TextBlock Grid.Row="2" Margin="30,20,30,0" FontSize="11" TextWrapping="Wrap" Foreground="#333333">
            By continuing, you agree to our policies.
        </TextBlock>
        
        <!-- Spacer -->
        <Grid Grid.Row="3"/>
        <Grid Grid.Row="4"/>
        
        <!-- Buttons -->
        <Grid Grid.Row="5" Background="#EEEEEE">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Margin="30,10,5,10" Content="Accept and Continue" Click="AcceptButton_Click" Background="#0066CC" Foreground="White" FontWeight="Bold"/>
            <Button Grid.Column="1" Margin="5,10,30,10" Content="Quit" Click="QuitButton_Click" Background="#CCCCCC" Foreground="#333333" FontWeight="Bold"/>
        </Grid>
    </Grid>
</Window>
